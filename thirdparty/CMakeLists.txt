# --------------------------------------------------------------------------------
#                      Third party libs setup
# --------------------------------------------------------------------------------
add_library(3RD_PARTY_LIBS INTERFACE)

# Check out the brach XXX example on how to add libraries here


# Linking the libraries to a single glob
target_link_libraries(3RD_PARTY_LIBS INTERFACE

)